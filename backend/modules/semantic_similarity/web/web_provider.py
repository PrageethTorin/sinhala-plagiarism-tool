#Responsible ONLY for getting Sinhala web text#

def fetch_web_paragraphs(query: str) -> list[str]:
    """
    Returns a list of Sinhala paragraphs from web or corpus
    """
    # later: crawler / corpus
    # for now: scraping / limited search
    return [
        "සිංහල භාෂාව ශ්‍රී ලංකාවේ ප්‍රධාන භාෂාවකි ...",
        "ශ්‍රී ලංකාවේ අධ්‍යාපන පද්ධතිය නවීකරණය වී ඇත ..."
    ]